streamlit>=1.25.0,<1.33  # Streamlit 버전 범위 최신화
torch>=2.0.0,<2.9       # Python 3.12 호환 가능 torch 범위 유지 또는 확장
sentence-transformers==2.2.2
transformers>=4.30.0,<4.39   # transformers 버전 유연성 확보
kss==3.5.6 # 명시적 버전 고정 또는 최신 안정 버전 테스트
python-docx==0.8.11
python-pptx==0.6.21
# textwrap3는 표준 textwrap 사용 시 불필요할 수 있음 (현재 코드에서는 표준 textwrap 사용)
# PyYAML은 명시적으로 사용되지 않으나, 다른 의존성으로 필요하다면 추가
PyYAML==6.0.1
numpy>=1.21.0
scipy>=1.7.3